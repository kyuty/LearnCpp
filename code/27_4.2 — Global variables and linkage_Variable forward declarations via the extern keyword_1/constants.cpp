//static const double g_gravity(9.8);     // 不能在其他文件使用 extern 找到该变量
//static double g_gravity(9.8);         // 不能在其他文件使用 extern 找到该变量
const double g_gravity(9.8);          // 不能在其他文件使用 extern 找到该变量
//double g_gravity(9.8);                // 能在其他文件使用 extern 找到该变量
